# Lambda-Cold-Start-Measurement
A measured comparison of three strategies for handling AWS Lambda cold starts. Three functions, the same DynamoDB workload, the same runtime and memory. Every figure below comes from a CloudWatch REPORT line — nothing modelled, nothing estimated.
